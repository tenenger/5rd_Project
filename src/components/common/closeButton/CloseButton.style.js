import styled from 'styled-components';

export const SLayout = styled.button`
  width: 4rem;
  height: 4rem;

  display: flex;
  align-items: center;
  justify-content: center;

  position: absolute;
  top: 0;
  right: 0;
  margin: 1rem;

  font-size: 4rem;
`;
